app {
  name = "EcommerceAnalytics"

  spark {
    master = "local[*]"
    log-level = "WARN"
    # Le défaut Spark (200) est surdimensionné pour ces volumes (500 à 100 000
    # lignes) et multiplie les petites tâches -> réduit pour un run local.
    shuffle-partitions = 8
  }

  data {
    input {
      transactions = "src/main/resources/data/transactions.csv"
      users        = "src/main/resources/data/users.json"
      products     = "src/main/resources/data/products.parquet"
      merchants    = "src/main/resources/data/merchants.csv"
    }
    output {
      path = "output"
    }
  }

  display {
    console-rows = 20
  }
}
