package com.ecommerce.analytics

/** Orchestration du pipeline complet (ingestion -> transformation -> analytique) -> Partie 6. */
object MainApp {
  def main(args: Array[String]): Unit = {
    println("EcommerceAnalytics - squelette de projet (Partie 1). Pipeline à implémenter dans les parties suivantes.")
  }
}
